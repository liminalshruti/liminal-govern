---
description: The 60-second taste of Liminal — three bounded agents read a sample brief, disagree, one refuses out of lane, you correct one read. The front door.
argument-hint: "[optional: your own brief instead of the sample]"
---

Invoke the `try-liminal` skill. Follow its SKILL.md flow exactly: run the taste runner, render the three agents' reads (lead with the in-lane agent, show refusals verbatim), capture one correction via the canonical taxonomy, then print the close line pointing at the desktop app.

User brief (optional): $ARGUMENTS
