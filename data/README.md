# data/ — seeded fixture (synthetic; no real names)

The demo runs on a seeded AI-usage governance dataset (not real-time ingestion). **Target fixture
(being built by the founder-brain session):** Claude usage by employee/model with task labels, GitHub
PRs, security-hardening tickets, an agent registry (incl. a cheaper verified `CalendarOps` agent), the
OKR baseline, and prior decisions — shaped so the demo's finding is real (e.g. ~11% of Opus 4.8 spend
on calendar/admin; security hardening under-allocated vs the 40% target; ≥1 task a cheaper agent does).

`sample-spend.csv` / `seat-activity.csv` / `q2-*.csv` are the initial SaaS-seat fixtures — repurposable
for the ROI dimension; superseded by the AI-usage governance fixture for the primary demo.
